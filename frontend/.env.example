# FastAPI brief endpoint. Optional in `next dev` — that already defaults to
# this URL. Set it in production (Vercel env) or to point at another host.
BRIEF_FORWARD_URL=http://127.0.0.1:8000/api/brief
